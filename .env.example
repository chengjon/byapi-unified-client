# Byapi Stock API Client - Environment Configuration Example
#
# Copy this file to .env and fill in with your actual license key(s)
# DO NOT commit .env to version control - it contains sensitive credentials

# REQUIRED: One or more license keys (comma-separated for automatic failover)
# Single key:
# BYAPI_LICENCE=your-license-key-here
#
# Multiple keys for automatic failover (recommended for production):
BYAPI_LICENCE=key1,key2,key3

# OPTIONAL: API Base URL (default: http://api.biyingapi.com)
# Use HTTP for development (faster), HTTPS for production (secure)
BYAPI_BASE_URL=http://api.biyingapi.com

# OPTIONAL: HTTPS Base URL (default: https://api.biyingapi.com)
BYAPI_HTTPS_BASE_URL=https://api.biyingapi.com

# OPTIONAL: Request timeout in seconds (default: 30)
# Increase if you're on a slow connection or API is responding slowly
BYAPI_TIMEOUT=30

# OPTIONAL: Logging level (default: INFO)
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
BYAPI_LOG_LEVEL=INFO

# OPTIONAL: Maximum number of retry attempts (default: 5)
# Failed requests will be automatically retried with exponential backoff
BYAPI_MAX_RETRIES=5

# OPTIONAL: Base delay for exponential backoff in seconds (default: 0.1)
# Actual delay = base * (2 ^ attempt_number) + jitter
BYAPI_RETRY_BASE_DELAY=0.1

# OPTIONAL: Maximum delay for retries in seconds (default: 30)
# Prevents waiting indefinitely on broken API
BYAPI_RETRY_MAX_DELAY=30

# OPTIONAL: Consecutive failure threshold (default: 5)
# License key marked as "faulty" after this many consecutive failures
BYAPI_CONSECUTIVE_FAILURES=5

# OPTIONAL: Total failure threshold (default: 10)
# License key marked as "invalid" and permanently disabled after this many total failures
BYAPI_TOTAL_FAILURES=10
