# Byapi 股票 API 客户端 - 环境配置示例
#
# 将此文件复制为 .env 并填入您的实际许可证密钥
# 请勿将 .env 提交到版本控制 - 它包含敏感凭据

# 必需：一个或多个许可证密钥（逗号分隔实现自动故障转移）
# 单个密钥：
# BYAPI_LICENCE=your-license-key-here
#
# 多个密钥实现自动故障转移（生产环境推荐）：
BYAPI_LICENCE=key1,key2,key3

# 可选：API 基础 URL（默认：http://api.biyingapi.com）
# 开发环境使用 HTTP（更快），生产环境使用 HTTPS（更安全）
BYAPI_BASE_URL=http://api.biyingapi.com

# 可选：HTTPS 基础 URL（默认：https://api.biyingapi.com）
BYAPI_HTTPS_BASE_URL=https://api.biyingapi.com

# 可选：请求超时秒数（默认：30）
# 如果网络较慢或 API 响应缓慢，可以增加此值
BYAPI_TIMEOUT=30

# 可选：日志级别（默认：INFO）
# 选项：DEBUG, INFO, WARNING, ERROR, CRITICAL
BYAPI_LOG_LEVEL=INFO

# 可选：最大重试次数（默认：5）
# 失败的请求将使用指数退避自动重试
BYAPI_MAX_RETRIES=5

# 可选：指数退避的基础延迟秒数（默认：0.1）
# 实际延迟 = 基础 * (2 ^ 尝试次数) + 抖动
BYAPI_RETRY_BASE_DELAY=0.1

# 可选：重试的最大延迟秒数（默认：30）
# 防止在 API 故障时无限等待
BYAPI_RETRY_MAX_DELAY=30

# 可选：连续失败阈值（默认：5）
# 许可证密钥在连续失败此次数后标记为“故障”
BYAPI_CONSECUTIVE_FAILURES=5

# 可选：总失败阈值（默认：10）
# 许可证密钥在总失败此次数后标记为“无效”并永久禁用
BYAPI_TOTAL_FAILURES=10